import classes from './Wishlist.module.css';

const Wishlist = () => {
    return (
        <div className={classes.wishlist}>
            <h1>Wishlist</h1>
        </div>
    )
};

export default Wishlist;